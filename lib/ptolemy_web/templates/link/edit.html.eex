<section class="box">
  <header class="box__header">
    <h1 class="box__title" title="<%= gettext "Editing link %{link}", link: @link.title %>">
      <%= gettext "Editing link %{link}", link: @link.title %>
    </h1>
  </header>
  <div class="box__body">
    <%= render "form.html", Map.put(Map.put(assigns, :action, Routes.link_path(@conn, :update, @link)), :method, :patch) %>
  </div>
</section>

